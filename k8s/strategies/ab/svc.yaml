apiVersion: v1
kind: Service
metadata: { name: ace-api-a }
spec:
  selector: { app: ace-api, variant: A }
  ports: [{ port: 80, targetPort: 5000 }]

---
apiVersion: v1
kind: Service
metadata: { name: ace-api-b }
spec:
  selector: { app: ace-api, variant: B }
  ports: [{ port: 80, targetPort: 5000 }]
